<!DOCTYPE html>
<html lang="en" dir="ltr">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width,initial-scale=1" />
    <title>{{ title + ' |' if title }} {{ site.title }}</title>
    <link rel="stylesheet" href="{{ 'styles.css' | r0bAsset | url }}" />
    <link rel="stylesheet" href="{{ '/static/css/custom.css' | url }}" />
    <link rel="icon" href="{{ 'png/favicon.png' | r0bAsset }}" />
    <link
      href="https://fonts.googleapis.com/css?family=Baloo+Bhai|Patrick+Hand&display=swap"
      rel="stylesheet"
    />
    <link
      rel="alternate"
      href="{{ '/feed.xml' | url }}"
      type="application/rss+xml"
      title="{{ site.title }}"
    />
    <meta
      property="og:title"
      content="{{ metaTitle or title or site.title }}"
    />
    <meta property="og:type" content="website" />
    <meta
      property="og:image"
      content="{{ site.url + 'static/img/opengraph.png' }}"
    />
    <meta property="og:url" content="{{ url }}" />
    <meta name="twitter:card" content="summary" />
    <meta name="twitter:site" content="{{ site.twitter }}" />
    <meta property="og:description" content="{{ metaSummary or summary }}" />
  </head>
  <body>
    {{ content | safe }}
  </body>
</html>
